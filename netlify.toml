[build]
  command = "chmod +x node_modules/.bin/* 2>/dev/null; npm run build"
  publish = "dist"
  functions = ".netlify/functions-internal"

[build.environment]
  NODE_VERSION = "22"

[[redirects]]
  from = "/*"
  to = "/.netlify/functions/server/:splat"
  status = 200
