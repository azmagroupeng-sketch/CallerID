import { a as parseIranPhone, s as toPersianDigits } from "./phone-CG57bSRI.js";
import { i as displayName, n as Route, o as useBook } from "./router-CdA9Lyn8.js";
import { t as ContactAvatar } from "./contact-row-NkS5TyGZ.js";
import { i as CallTimeline, n as PhonePanel, t as Textarea } from "./textarea-CXInE3v5.js";
import { n as Button, t as Input } from "./input-D-ATWlHF.js";
import { t as Label } from "./label-2gHoGVxj.js";
import { useMemo, useState } from "react";
import { Link, useNavigate } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { ArrowRight } from "lucide-react";
import { toast } from "sonner";
//#region src/routes/book.$id.tsx?tsr-split=component
function ContactPage() {
	const { id } = Route.useParams();
	const ready = useBook((s) => s.ready);
	const contacts = useBook((s) => s.contacts);
	const allCalls = useBook((s) => s.calls);
	const contact = contacts.find((c) => c.id === id);
	const calls = useMemo(() => allCalls.filter((c) => c.contactId === id).slice().sort((a, b) => b.at - a.at), [allCalls, id]);
	if (!ready) return /* @__PURE__ */ jsx("div", { className: "h-40 rounded-xl bg-card" });
	if (!contact) return /* @__PURE__ */ jsxs("div", {
		className: "py-10 text-center",
		children: [
			/* @__PURE__ */ jsx("h1", {
				className: "text-lg font-semibold",
				children: "مخاطب پیدا نشد"
			}),
			/* @__PURE__ */ jsx("p", {
				className: "mt-2 text-sm text-muted-foreground",
				children: "شاید حذف شده باشد."
			}),
			/* @__PURE__ */ jsx(Link, {
				to: "/book",
				className: "mt-5 inline-flex h-11 items-center rounded-xl bg-primary px-4 text-sm font-medium text-primary-foreground",
				children: "بازگشت به دفترچه"
			})
		]
	});
	const parsed = parseIranPhone(contact.local);
	return /* @__PURE__ */ jsxs("div", {
		className: "space-y-5",
		children: [
			/* @__PURE__ */ jsxs(Link, {
				to: "/book",
				className: "inline-flex h-10 items-center gap-1 text-sm text-muted-foreground hover:text-foreground",
				children: [/* @__PURE__ */ jsx(ArrowRight, { className: "size-4" }), "دفترچه"]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "flex items-center gap-3",
				children: [/* @__PURE__ */ jsx(ContactAvatar, { contact }), /* @__PURE__ */ jsxs("div", {
					className: "min-w-0",
					children: [/* @__PURE__ */ jsx("h1", {
						className: "truncate text-lg font-semibold tracking-tight",
						children: displayName(contact)
					}), /* @__PURE__ */ jsxs("p", {
						className: "text-xs text-muted-foreground",
						children: [toPersianDigits(calls.length), " تماس ثبت‌شده"]
					})]
				})]
			}),
			/* @__PURE__ */ jsx(PhonePanel, { parsed }),
			/* @__PURE__ */ jsx(NewCallForm, { contact }),
			/* @__PURE__ */ jsxs("div", {
				className: "rounded-xl bg-card px-4 py-4 ring-1 ring-border",
				children: [/* @__PURE__ */ jsx("p", {
					className: "mb-4 text-sm font-medium",
					children: "سابقه با تاریخ شمسی"
				}), /* @__PURE__ */ jsx(CallTimeline, { calls })]
			}),
			/* @__PURE__ */ jsx(EditContactForm, { contact }),
			/* @__PURE__ */ jsx(DeleteContactButton, { contact })
		]
	});
}
function NewCallForm({ contact }) {
	const addCall = useBook((s) => s.addCall);
	const [reason, setReason] = useState("");
	async function saveCall(event) {
		event.preventDefault();
		if (!reason.trim()) {
			toast.error("علت تماس را بنویسید");
			return;
		}
		try {
			await addCall(contact.id, reason);
			setReason("");
			toast.success("علت تماس ثبت شد");
		} catch (err) {
			toast.error(err instanceof Error ? err.message : "ثبت ناموفق بود");
		}
	}
	return /* @__PURE__ */ jsxs("form", {
		onSubmit: saveCall,
		className: "space-y-3 rounded-xl bg-card p-4 ring-1 ring-border",
		children: [
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-1.5",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "detail-reason",
					children: "علت تماس جدید"
				}), /* @__PURE__ */ jsx(Textarea, {
					id: "detail-reason",
					value: reason,
					onChange: (e) => setReason(e.target.value),
					placeholder: "این بار برای چه موضوعی تماس گرفت؟"
				})]
			}),
			/* @__PURE__ */ jsx(Button, {
				type: "submit",
				className: "w-full",
				children: "ثبت تماس تازه"
			}),
			/* @__PURE__ */ jsx(Button, {
				type: "button",
				variant: "secondary",
				className: "w-full",
				asChild: true,
				children: /* @__PURE__ */ jsx(Link, {
					to: "/",
					search: { n: contact.local },
					children: "باز کردن در تماس تازه"
				})
			})
		]
	});
}
function EditContactForm({ contact }) {
	const updateContact = useBook((s) => s.updateContact);
	const [lastName, setLastName] = useState(contact.lastName);
	const [firstName, setFirstName] = useState(contact.firstName);
	const [company, setCompany] = useState(contact.company);
	async function saveProfile(event) {
		event.preventDefault();
		if (!lastName.trim()) {
			toast.error("نام خانوادگی را وارد کنید");
			return;
		}
		try {
			await updateContact(contact.id, {
				lastName,
				firstName,
				company
			});
			toast.success("اطلاعات مخاطب ذخیره شد");
		} catch (err) {
			toast.error(err instanceof Error ? err.message : "ذخیره ناموفق بود");
		}
	}
	return /* @__PURE__ */ jsxs("form", {
		onSubmit: saveProfile,
		className: "space-y-3 rounded-xl bg-card p-4 ring-1 ring-border",
		children: [
			/* @__PURE__ */ jsx("p", {
				className: "text-sm font-medium",
				children: "ویرایش مخاطب"
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-1.5",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "edit-last",
					children: "نام خانوادگی"
				}), /* @__PURE__ */ jsx(Input, {
					id: "edit-last",
					value: lastName,
					onChange: (e) => setLastName(e.target.value)
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-1.5",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "edit-first",
					children: "نام"
				}), /* @__PURE__ */ jsx(Input, {
					id: "edit-first",
					value: firstName,
					onChange: (e) => setFirstName(e.target.value)
				})]
			}),
			/* @__PURE__ */ jsxs("div", {
				className: "space-y-1.5",
				children: [/* @__PURE__ */ jsx(Label, {
					htmlFor: "edit-company",
					children: "شرکت"
				}), /* @__PURE__ */ jsx(Input, {
					id: "edit-company",
					value: company,
					onChange: (e) => setCompany(e.target.value)
				})]
			}),
			/* @__PURE__ */ jsx(Button, {
				type: "submit",
				variant: "secondary",
				className: "w-full",
				children: "ذخیره تغییرات"
			})
		]
	});
}
function DeleteContactButton({ contact }) {
	const removeContact = useBook((s) => s.removeContact);
	const navigate = useNavigate();
	const [confirmDelete, setConfirmDelete] = useState(false);
	async function onDelete() {
		if (!confirmDelete) {
			setConfirmDelete(true);
			return;
		}
		try {
			await removeContact(contact.id);
			toast.success("مخاطب حذف شد");
			navigate({ to: "/book" });
		} catch (err) {
			toast.error(err instanceof Error ? err.message : "حذف ناموفق بود");
		}
	}
	return /* @__PURE__ */ jsx(Button, {
		type: "button",
		variant: "ghost",
		className: "w-full text-destructive hover:bg-destructive/10 hover:text-destructive",
		onClick: onDelete,
		children: confirmDelete ? "تأیید حذف مخاطب و سابقه" : "حذف مخاطب"
	});
}
//#endregion
export { ContactPage as component };
